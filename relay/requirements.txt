# Pinned to a known-good set (Python 3.10+).
# pywebpush + py-vapid are only needed if you enable lock-screen push;
# they're harmless to install regardless (the relay degrades gracefully).
fastapi==0.137.1
uvicorn==0.49.0
httpx[http2]==0.28.1   # http2: APNs 只说 HTTP/2（2026-08-07 原生推送）
cryptography           # APNs provider JWT 的 ES256 签名（bark 加密也用它，之前靠碰运气）
pywebpush==2.3.0
py-vapid==1.9.4
# official-app connector: only used when RELAY_MCP_PATH is set
mcp==1.19.0
